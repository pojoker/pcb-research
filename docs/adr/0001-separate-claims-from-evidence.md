# 主张与证据分表，支持边与限界边并存

知识图谱将概念、可证伪主张、披露件和证据关系分别存储；证据关系显式区分 `supports`、`refutes`、`limits` 与 `context_only`。检索活动与尚未适用域匹配的候选披露另存为 search log，`no_candidate_found` 永远不能自动生成反驳边。这样同一份官方资料既能支持一个窄命题，也能限制由它外推出来的过宽结论，避免把“有引用”或“没有搜到”误当成主张已被证明。

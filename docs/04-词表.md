# 04 词表候选登记册（PCB）· v1（红队返修版）

> 实例化第 4 步。目标格式：`term | match_mode | include_patterns | exclude_patterns | target_cell | case_policy | test_fixture_ids`（结构化词表）。
> **纪律：每个词必须登记真实可编译的排除正则；高危词配正例/反例/边界例 fixtures；入表前实测语料四键（关键词/范围/日期/命中数）；裸缩写默认不可入表。**
> 本册全部条目状态 = 未实测。
> v1 返修（codex 重要12/13 + cursor B3/I8）：新增保留字规则；补 SAP/PI/EMC/CCL/ABF/PCB 等高危词；排除正则从"登记文字"升级为可执行规则。

## 保留字（最高危，先于一切词表工作）

- **格 ID 是保留字**：FAB1/FAB2/M1-M5/M7/M9/MSK/FLX/PM1-3/P1-9/EQ1-7——禁止作为词表词形，扫描脚本须校验冲突（不变量⑲）。
- **M4/M6/M8 是损耗等级市场俗称**（源自松下 MEGTRON 泛化）：禁止作格 ID、禁止裸词入表；表达用"低损耗/超低损耗 + 料号"（MEGTRON6/MEGTRON8/EM-890K 等，料号逐条登记）。
- 开新格先查保留字表。

## 高撞车风险（裸缩写禁止，须短语化或强上下文；排除正则须真实可编译）

| 词 | 拟挂格 | 撞车风险 | 处置 |
|---|---|---|---|
| PCB | ANY | **多氯联苯（环境污染物）** | 排除正则：`多氯联苯|污染物|斯德哥尔摩|公约` |
| SAP | P5 | 德国软件公司 | 禁裸词；只收 `mSAP`、`SAP制程`、`半加成` |
| HDI | FAB1 刻面 | 六亚甲基二异氰酸酯 | 至少两个结构信号：`阶|微盲孔|任意层|线路` 中命中二；排除 `异氰酸酯|聚氨酯|固化剂` |
| PI | FLX | 多义（圆周率/PI 等） | 禁裸词；用 `PI膜`、`聚酰亚胺` |
| MPI | FLX | 并行计算接口 | 上下文须含 `软板|基材|改性聚酰亚胺` |
| EMC | M9 | 电磁兼容 | 禁裸词；用 `环氧塑封料`，或上下文须含 `封装材料|塑封` |
| CCL | M1 | 多义缩写 | 上下文须含 `覆铜|层压板|玻纤` |
| FCCL | FLX | 多义 | 上下文须含 `挠性|软板|聚酰亚胺` |
| ABF | M9 | Associated British Foods 等 | 上下文须含 `载板|味之素|积层膜` |
| BT | M9 | BT 下载、**Bluetooth 蓝牙** | 禁裸词；用 `BT树脂`、`BT载板`；排除 `蓝牙|Bluetooth|下载` |
| PP | M2 | 聚丙烯/PowerPoint | 禁裸词；用 `半固化片`、`粘结片` |
| MLB | FAB1 | 棒球 | 禁裸词；用 `多层板` |
| M6/M8 | 轴D | 宝马 M8、格 ID 保留字 | 见"保留字"节 |
| VCP | EQ3 | VMware 认证 | 排除 `VMware|虚拟化|认证`；上下文须含 `电镀` |
| AOI | EQ5/P9 | GIS 兴趣点 | 上下文须含 `检测|光学`；排除 `兴趣点|地图|GIS` |
| OSP | P7 | 开放服务平台等 | 上下文须含 `表面处理|焊接|可焊` |
| SLP | FAB1 刻面 | sleep 等 | 禁裸词；用 `类载板` |
| RTF | M3 | 富文本格式 | 上下文须含 `铜箔|反转` |
| PPO | M4 | 美国医保组织 | 用 `聚苯醚`，或上下文须含 `树脂` |
| LCP | FLX | 多义 | 上下文须含 `软板|液晶聚合物` |
| Dk/Df | 轴D | 多义 | 上下文须含 `介电|损耗` |
| COF | FLX | 多义 | 上下文须含 `覆晶|软板|驱动` |
| CAM/DFM | 工序支持 | 计算机辅助/多义 | 上下文须含 `制前|工程|光绘` |
| EMS/ODM/OEM | 角色词 | 泛用 | 不挂格；仅作角色/不等式语境词 |
| SMT/BGA | 树外 | 贴装语境 | 树外词；命中按 PCBA 邻居处理，不入 FAB |
| FR-4/Tg | M1 | 多义 | 上下文须含 `基材|玻纤|玻璃化` |
| IPC/UL | 认证词 | 泛用 | 上下文须含 `标准|认证`；不挂格 |
| RF | 轴A | 泛用 | 上下文须含 `射频|高频` |
| 黑化 | P1 | 网文用语 | 上下文须含 `内层|氧化|棕化` |
| 金手指 | P8 | 俚语/小说 | 上下文须含 `PCB|连接器|倒角` |

**M3 铜箔词统一纪律**：正向上下文 `电子电路|覆铜板|板厂` + 负向正则 `电池|负极|动力|锂电|储能`，缺一直接驳回-非本格。

## 低风险词（仍须实测）

- 品类：单面板、双面板、多层板、HDI 板、刚挠结合、挠性板、类载板、封装基板、IC 载板、金属基板、背板、厚铜板
- 材料：覆铜板、半固化片、电解铜箔、压延铜箔、载体铜箔、低轮廓铜箔、HVLP、玻纤布、电子布、硅微粉、球形硅、聚四氟乙烯、PTFE、碳氢树脂、聚苯醚、聚酰亚胺、PI 膜、电磁屏蔽膜、覆盖膜、纯胶、补强板、阻焊油墨、干膜、沉铜、电镀铜光剂、金盐、氰化亚金钾、环氧塑封料
- 工艺：背钻、树脂塞孔、盘中孔、VIPPO、盲埋孔、任意层互连、mSAP、半加成、加成法、脉冲电镀、填孔电镀、沉金、ENIG、沉银、沉锡、喷锡、HASL、棕化、压合、镭射钻孔、激光钻孔、厚径比、纵横比、阻抗控制、混压、埋嵌铜块
- 设备/检测：LDI、激光直写、曝光机、机械钻机、垂直连续电镀、龙门线、水平线、真空压机、飞针测试、通用测试、X-Ray、AOI 检测、AVI、锣机、V-cut
- 下游：AI 服务器、加速卡、OAM、UBB、交换机、基站、ADAS、三电、域控制器

## 入表流程

1. 候选登记（本册）→ 2. 正则编译闸（re 可编译，不过不入表）→ 3. fixtures 配置（正例/反例/边界例）→
4. 语料实测四键（关键词/范围/日期/命中数）→ 5. 排除正则实测复跑 → 6. 判定闸批准入词表 → 7. 撞车前科进判例库。
